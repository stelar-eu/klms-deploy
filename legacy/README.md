# Legacy deployment scripts

`bootstrap.py` is the deprecated pre-`stelarctl` deployment helper. It is kept
here only for maintaining older installations that still depend on the previous
`bootstrap.yaml` flow.

New deployments should use the supported `stelarctl` workflow documented in
`docs/stelarctl.md` and `DEPLOY.md`.
